spring:
  datasource:
    url: jdbc:postgresql://36.50.176.136:5432/ecommerce
    username: whoami
    password: Whoami123!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
